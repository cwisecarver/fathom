# django-libsql validation harness — pinned to the versions proven against fathom.
#
# Python: use 3.12. Python 3.13 deprecated and 3.14 REMOVED sqlite3.dbapi2.version,
#   which libsql-client 0.3.1 imports at module load (fails on import, not install).
# Django: pinned to 5.0.x. django-libsql 0.1.3's schema editor references
#   model._meta.index_together, which Django REMOVED in 5.1 — so 5.1+ breaks on the
#   first alter_field migration (e.g. the auth chain).
Django==5.0.6
django-libsql==0.1.3
libsql-client==0.3.1

# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Benchmark run logs (scripts/benchmark.sh). perf_history.jsonl is tracked.
/logs/

# Python virtualenv for the django-libsql validation harness (test/django_validation).
/.venv/

# Python bytecode artifacts.
__pycache__/
*.pyc

# Ignore package tarball (built via "mix hex.build").
fathom-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# Local working notes and plans (see AGENTS.md §Workflow) — kept out of the repo.
/tasks/

# Local AI-assistant configuration and permissions.
/.claude/

# Wire-bench (test-env) history — generated by `mix fathom.wire_bench`, not the canonical
# prod perf record. A committed canonical baseline is a follow-on decision.
/scripts/wire_history.jsonl

# TPC-C sweep history — generated by `mix fathom.tpcc`, recorded-only (never a gate) and
# host-specific, so it stays local like wire_history (only perf_history.jsonl is tracked).
/scripts/tpc_history.jsonl

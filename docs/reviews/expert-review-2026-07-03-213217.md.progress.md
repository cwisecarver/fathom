# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-03-213217.md
**Started:** 2026-07-03
**Last updated:** 2026-07-04
**Mode:** #28-first then autonomous top-down (complete). All 31 well-scoped findings FIXED; #4, #6, #7, #9, #19, #26 GATED awaiting the batched architectural proposal.

| # | Status | Summary |
|---|--------|---------|
| 1 | FIXED | ae20e15 — download retry:false + self-retry with fresh temp; checksum mismatch retryable |
| 2 | FIXED | e9030fe — data-PUT 412 reconciles via check_lease; own-PUT keeps serving, genuine steal keeps local |
| 3 | FIXED | 1defa5a — heartbeat_path percent-encodes owner (both backends); owner_live? verifies owner match |
| 4 | FIXED | 0189719 — alternating-form touch (single↔one-part-multipart), HEAD-confirmed rotation, boot probe both directions; MD5-faithful S3EtagStore test double |
| 5 | FIXED | d2ba98b — migrator forward flush If-Match-fenced (env-skip; restore path deferred to #4/#8) |
| 6 | FIXED | 1458222 — touch returns {pre, post} threaded through the lease; warm adopt only when sidecar == pre, else quarantine+re-pull; no extra HEAD; end-to-end zombie-launder test (note: one word backtick-mangled in the commit body)|
| 7 | FIXED | 5198875 — nonced create-fence sentinel (If-None-Match:*, meta marker); pull recognizes as brand-new carrying its etag; second steal refreshes; follower reads absent |
| 8 | FIXED | 309a8ab — copy_body_ok? treats 2xx+<Error> as failure (touch/copy); 5GB multipart deferred |
| 9 | FIXED | 3070f59 — statement_chain (current+1)..target from live user_version; Copy.migrate_chain per-step txn+stamp; yanked intermediate fails closed; v1→v3 test. A/B-documented [skip-bench] (cold_open floor drifted overnight; clean tree +26.8%) |
| 10 | FIXED | c52d594 — next_version/0 (max incl. yanked) replaces head()+1 in capture; yank-then-capture regression test |
| 11 | FIXED | 4e4018e — generation-anchored watermark (counter_gen/flush_pending_gen); mismatch ⇒ dirty; parked-flush + counter-restart regression test |
| 12 | FIXED | a6bf39b — read_etag_sidecar: empty/unreadable = :corrupt → quarantine; only enoent is legacy adopt-once |
| 13 | FIXED | 4ef69de — post-copy identity check (inode/mtime/size + sidecar re-read); doubt ⇒ cold pull; FaultyStorage :promote hook |
| 14 | FIXED | baeebd7 — unique .forked.<ms>-<uniq> names, load-bearing main rename; failure ⇒ not-quarantined (warm-fenced/open-fails, never overwritten). Documented [skip-bench], A/B: clean tree blocked +214% same window (VM load) |
| 15 | FIXED | 89e3143 — init broadcasts the bumped generation like mark_lapse; restart test asserts delivery |
| 16 | FIXED | 153c268 — proof-of-death clear: stale ⇒ boot-clear, fresh ⇒ frozen-expiry re-check (dead) vs advancing (refuse), unreadable fails closed |
| 17 | FIXED | c696e17 — cancel_retirement at top of RevertJob.perform + RetirementJob revert-in-flight guard (any live Oban state) |
| 18 | FIXED | aa0a3f5 — settle_yield_ms = min(30s, budget/3); ≥2/3 of shutdown budget reserved for the drop-flush |
| 19 | FIXED | 25e9e64 — post_lease_warm_check on every warm open (one HEAD post-acquire): moved lineage ⇒ quarantine+re-pull; vanished ⇒ nil fence; bench-gated flat |
| 20 | FIXED | c51bad6 — restore_lock rolls a failed steal back to the dead owner's lock (If-Match); retry redoes full steal+touch; req_plug S3-double test |
| 21 | FIXED | 3d3b501 — retarget_inflight_reverts rewrites {force, to_version} (last command wins); guard refusal reloads row args and snoozes on a mid-execution upgrade |
| 22 | FIXED | 3f1bc31 — yank cancels all live states incl. executing; ReconcileJob → revert_stranded/0 converges yanked-above-HEAD shards |
| 23 | FIXED | f9c6504 — unknown_version cancels without mark_failed; lazy path maps it to :ok (serve old version) |
| 24 | FIXED | e5c7604 — Oban.Notifier (Postgres LISTEN/NOTIFY, not PubSub — no BEAM cluster) pushes revocations fleet-wide; put_max downgrade guard |
| 25 | FIXED | f6347b3 — stale-but-safe floor on read error; :hrana_revocation_on_error posture (:fail_open default / :fail_closed → :unavailable); floor_error telemetry |
| 26 | GATED | lapse-broadcast thundering herd — fence refactor |
| 27 | FIXED | ceb7d23 — download try/after; Storage.reap_stale_temps (age-gated .dl/.snap/.tmp/.pull) at open + follower refresh; +GC to keep fanout flat (16.9 KiB/shard) |
| 28 | FIXED | 00aef48 + f7bd031 — atomic write_lock (mutex removed: :global.trans contended the fence hot path); two-flush race test; lease-steal tests made deterministic |
| 29 | FIXED | 37d771d — vanished-object takeover fences nil (If-None-Match recreate) + drops stale sidecar |
| 30 | FIXED | f9be0b2 — completed-revert retry returns :ok before touching storage; backup byte-identical test |
| 31 | FIXED | 7a92fac — task starts Repo + probes the floor read; unreadable ⇒ loud Mix.raise, never a maybe-dead token |
| 32 | FIXED | 5664466 — register with on_conflict: :nothing (no recency bump); {:ok, v} | {:error, changeset} instead of MatchError |
| 33 | FIXED | d613ffe — op-tagged grants; abandon drops exactly the timed-out call's grant (bench-gated, flat) |
| 34 | FIXED | 11f5ebe — mark_incarnation_dead after #16's proof-of-death; owner_live? treats exactly that owner as dead (both backends) |
| 35 | FIXED | 475c3dc — blank env trimmed to unset in runtime.exs + zone_matches? belt; isolation test |
| 36 | FIXED | 5f84fc3 — check_config! warns on :required with no :hrana_token_secret (the documented warning) |
| 37 | FIXED | 1acb3e9 — dead ttl_ms option removed; :hrana_revocation_ttl_ms app env is the knob |

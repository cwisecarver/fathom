# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-19-235232.md
**Started:** 2026-07-20
**Last updated:** 2026-07-22

Deferred by triage: #4 (re-pullable data + optional S3 versioning, no code), #5 (runbook note), #10, #11 (marked DEFERRED in the findings file).

**Numbering note (2026-07-21):** the `#` column below now follows the SOURCE review file's ranking. An earlier drift labeled the LRU busy-filter fix "#14" (commit `c3f6ff3` title) when it is the review's **#16**; the review's true **#14** (flush_now timeout → false :ok) was skipped and is fixed here in `a24c25b`. Trust the review-file number, not the commit-title number, for these three (14/15/16).

| # | Status | Summary |
|---|--------|---------|
| 1 | FIXED | snapshot_id gated (cast_snapshot_id) in restore/3 + drop/2; regression test; 6cfef7e |
| 2 | FIXED | new terminate/2 clause flushes dirty conns>0 shutdown (tombstone-gated for delete); 2 regression tests; verified fail-pre-fix |
| 3 | FIXED | WriteFence circuit-breaker: coordinator fences writes when provably stealable (not-valid > ttl+margin), executor refuses writes (503 FILO_STALE_LEASE) but serves reads, lifts on recovery; gate on-in-prod; end-to-end test |
| 6 | FIXED | cross-store DR coherence (full scope). 6a storage tombstone backstop (9d349b1), 6b storage token-floor backstop (34eac07), 6c/6d `mix fathom.directory reconcile` + schema_version-from-user_version (1d7e1fe), 6e DR runbook |
| 7 | FIXED | snapshot restore schema-version guard: pull_snapshot reads the snapshot's user_version, refuse a cross-version restore without force:true, reconcile the directory to the restored version on success (laggard converges forward); fails open on Postgres error; API + tests |
| 8 | FIXED | scoped/revocable/per-identity control-plane API keys (Fathom.ApiKeys, SHA-256 hashed, read<manage<destroy), :api_auth plug + per-action require_scope, legacy BasicAuth fallback, mix fathom.apikey; e3c8519 |
| 9 | FIXED | append-only audit_events + Fathom.Audit: every mutating API action (action/2 override), admin export, and dashboard delete/suspend/resume records actor/action/shard/ip/outcome + telemetry; best-effort |
| 12 | FIXED | steal-time etag touch re-sends x-amz-meta-fathom-md5 (single_copy_touch REPLACE + create_multipart) so failover-pull integrity verification survives the touch; S3EtagStore models REPLACE-metadata; 2 tests (both touch directions), verified fail-pre-fix |
| 13 | FIXED | steal decision (owner_live?) compares against S3's response Date (shared clock) not the reader's local clock, so a forward reader-clock step can't wrongfully steal a live owner; + clock-skew telemetry. Chose S3-Date over the finding's double-read (no ~ttl/3 RTO tax); owner-clock skew noted as double-read follow-up. Tests + store Date modeling |
| 14 | FIXED | Shards.flush/1 blanket `catch :exit, _ -> :ok` swallowed the flush_now TIMEOUT exit, so a hung/slow store returned :ok while the shard was non-durable and a fork-before-flush cloned stale bytes. Narrowed: {:timeout,_} → {:error,:flush_timeout}, :ok only for :noproc/:normal/:shutdown, else {:error,{:flush_exited,_}}; flush_now timeout config-overridable (:flush_now_timeout_ms) for testability; :sys.suspend regression test (fails pre-fix :ok / passes post-fix); a24c25b (bench-gated), clean baseline this commit. **[review #14 — was skipped in the drift]** |
| 15 | FIXED | cold crash-steal takeover adopts the touched etag (revalidate_touched cold branch: etag == touch_pre_etag ⇒ {:ok, post} + sidecar stamp) instead of a full-body re-pull — the warm standby's headline failover no longer discards the copy it holds; failover_rto bench now seeds a dead-owner lock so cold+warm samples measure the real steal+touch path; regression test (real coordinator over S3EtagStore, touch held until pull GET serves, counts full-body GETs: pre-fix 2 / post-fix 1) closes the #30 cold-path coverage gap; a2202af (bench-gated), clean baseline aef2372 |
| 17 | FIXED | flush-storm defenses (both config-gated): (1) jitter — schedule_flush spreads each delay by ±:shard_flush_jitter_ratio (default 0.25, 0 disables) so a mass re-home's phase-aligned 5s timers drift apart; (2) node-wide concurrent-flush cap — new Fathom.Shard.FlushGate (lock-free ETS counter, :shard_flush_max_concurrency, default nil=unbounded): coordinator reserves a slot before spawning a flush task, over-cap backs off + stays dirty (safe direction), slot released at completion/crash/terminate (flush_slot_held). Tests: FlushGate unit + coordinator-backoff + jitter-range (jitter=0 in test config); verified backoff coupled to gate, jitter test fails without jitter. 55a0570 (bench-gated), clean baseline this commit |
| 16 | FIXED | LRU eviction skips busy shards: coordinator publishes map_size(conns) to a Lru busy table on grant/release (+ touch-on-release), lru_order filters busy BEFORE the limit cut so a bounded probe reaches idle candidates past a busy front (soft cap no longer degrades to hard cap under long-lived streams); unit + wiring tests. **[shipped as commit `c3f6ff3` titled "audit #14" — this is the review's #16]** |
| 24 | FIXED | backup verification was on-demand only → a bad stored object on a dormant tenant's cold tail surfaced when the tenant returned. Fix: Fathom.RestoreDrillJob, a gated fleet-singleton daily Oban cron that samples least-recently-verified active shards (Directory.sample_for_drill, ASC NULLS FIRST) + pulls + classifies each durable object (ok/corrupt/schema_mismatch/sentinel/absent/error), stamps last_verified_at + last_verify_status (migration), emits [:fathom,:restore_drill,:result] telemetry + FathomRestoreDrillFailure page. Off by default (:restore_drill_sample). Deliberately NO last_verified_at index (would tax resolve/record hot path ~15% for a gated daily query — dropped after measuring; documented). 6 tests + migration reversibility. 76a15f2 (bench-gated, dir_resolve back in noise band). |
| 23 | PARTIAL (core tooling shipped; directory-table + heartbeat-janitor deferred) | quarantine files preserved acked-but-unflushed/corrupt copies with no inventory, recovery, or retention (unbounded local-disk leak). Shipped: `mix fathom.shard quarantines` (local scan: shard/kind/age/size) + `quarantine-diff <file> <shard>` (per-table row deltas vs live, artifact copied so untouched); TempReaper age-caps them (:quarantine_retention_ms, default 30d, 0=forever) + emits fathom.shard.quarantines GAUGE; Fathom.Shard.quarantine_files/1 enumerator. 3 tests + inventory smoke. 848e913 (bench-gated). DEFERRED: shard_quarantines directory table (survives node / joins loss report) + stale-heartbeat/* object janitor (the harmless coordination-object litter). |
| 22 | FIXED | Tenants.export had no flush-first: a portability/offboarding export silently omitted a write-active tenant's newest writes (up to the flush interval, a whole session in idle-only mode) — a GDPR Art.20 defect — and never integrity-checked the bytes. Fix: export(shard_id, opts) with flush: true DEFAULT (Shards.flush -> flush_now, rides #14 so a timeout surfaces not masks), flush: false keeps old behavior; verify_integrity/1 on the pulled temp refuses a corrupt object ({:error, {:corrupt_export, _}}); admin Export inherits the default + maps errors to 500/503. 3 tests (default captures buffered write / flush timeout fails export / corrupt refused). 690a87a [skip-bench] |
| 21 | PARTIAL (lever 1 + docs; lever 2 deferred by owner) | hard-crash failover RTO. Lever 1 (shipped): Shards.checkout holds + retries a {:shard_held} when the holder is a CRASHED node (heartbeat frozen, aging out within :crash_failover_hold_ms, default 5s) — converts the TTL-window TAIL to latency, never holds for a LIVE holder (holder_stealable_soon? via a heartbeat read), fathom.shards.crash_wait telemetry. RTO ≈ ttl+steal_margin floor + :shard_lease_ttl_ms knob documented in docs/single-writer.md. 2 tests (crash-tail serve / live-holder immediate error). 4364d36 (bench-gated). Lever 2 (dead-node takeover sweeper — the real fix) DEFERRED per owner: needs a shard→owner signal the directory doesn't record (ownership is in the S3 lock). |
| 20 | FIXED | flip-before-drain client-error window: a handoff flips the LB to the target before the source drains, so the target's acquire is {:shard_held, source} for the drain window and retry_checkout? excludes :held → tenant-visible errors on the hottest shard. Fix: Fathom.Shards.checkout, on {:shard_held}, checks Overrides for a pin of this shard TO THIS NODE; if so, holds + retries the acquire (capped backoff 50ms→1s) up to :handoff_held_retry_budget_ms (default 10s), so post-flip requests queue then serve. Fails closed on Postgres doubt; one Overrides read per held-checkout; inert unless rebalancing armed. fathom.shards.handoff_wait telemetry. 3 tests (queue-then-serve / non-handoff immediate error / bounded exhaustion). 134640c (bench-gated), clean baseline this commit |
| 19 | FIXED | CommandPoller batch-task in-flight leak: dispatch relied on the detached task's try/after to send {:batch_done}, which an exit SIGNAL (TaskSupervisor restart / kill) skips → ids stuck in in_flight forever, command wedged `pending`. Fix: Process.monitor the batch task, free ids on {:DOWN} (batches: ref⇒ids); batch_done = fast path, monitor = guaranteed; in-flight now marked only after a confirmed start_child (latent leak too); orphan recovery logs + emits fathom.rebalancer.command.orphaned. Regression test kills the blocked batch task, asserts re-selection (fails pre-fix). f0a7c65 [skip-bench] |
| 18 | FIXED | Oban control-plane liveness gauges (the exception counter catches jobs that FAIL; these catch jobs that DON'T RUN — paused/backlogged queue, wedged singleton cron leader). New Fathom.Admin.Measurements.oban_health/0 on a 30s Admin-gated poller (only measurement touching Postgres, never in test): per-queue available/retryable depth + oldest-runnable age (fathom.oban.queue.*), per-cron freshness = secs since last insert (fathom.oban.cron.age_ms, workers from the crontab config). 3 alert rules (FathomObanQueueBacklog + Rebalance/Reconcile CronStalled at 2× period). 3 tests over seeded oban_jobs. cd484e4 [skip-bench] (observability, not a hot path) |
| 28 | PARTIAL (core lever shipped; chaos validation deferred) | no node-level graceful drain → every deploy funneled through the crash-adjacent supervisor-shutdown path. Fix: Fathom.Shards.drain_all(budget_ms) — marks node draining (HealthPlug 503 so the LB deregisters first), fans out request_drain across every open coordinator (reuses await_coordinator_exit) with bounded concurrency + bounded overall budget, returns %{drained,busy,timed_out}; a busy shard is reported not cut. HealthPlug persistent_term draining flag. Pre-stop wiring (bin/fathom rpc) + knobs documented in docs/runbooks/cluster.md. 3 tests (drain idle / busy-not-cut / health 200↔503). 4a0f9c9 (bench-gated). DEFERRED: chaos-rig rolling-deploy validation + mix fathom.rpo deploy-window measurement. |
| 30 | FIXED | the takeover-revalidation lattice (revalidate_takeover → revalidate_touched / post_lease_warm_check, the branchiest/most safety-critical fence logic) had zero DEDICATED coverage — it only rotates a real etag against real S3, so it was validated only by the boot probe + chaos runs. New test/fathom/shard/revalidate_touched_test.exs drives a real coordinator over the MD5-faithful S3EtagStore (warm? set by pre-placing the live file; a dead lock forces the steal+touch; S3EtagStore.set_body/3 models an out-of-band flush between two reads) and pins every branch: revalidate_touched — etag==post fast path (A), warm-adopt-on-provenance (B1), warm zombie-flush-diverged quarantine (B2), cold-diverged re-pull (D); post_lease_warm_check — unchanged (P1), object-gone (P2), release-in-gap fork (P3), unreachable (P4). Cold etag==pre #15 adoption stays covered by steal_touch_warm_standby_test.exs; the legacy/no-touch re-pull by takeover_revalidation_test.exs. Each test discriminates its branch by full-body GET count / served lineage / sidecar contents / .forked presence. Verified they bite: neutering the warm provenance guard fails B2 (not B1); neutering the release-in-gap branch fails P3 (not P1). Two :quarantine_failed sub-branches deliberately not forced (flaky rename failure). 8 tests. [skip-bench] (test-only). |
| 33 | FIXED | Tombstones/Suspensions boot-load rescued a Postgres failure to an EMPTY deny set then waited the full 5-min refresh before retrying — a window (Postgres wobble coincident with a GenServer restart) where a deleted tenant re-mints (410 breaks) and a suspended one serves (403 breaks). Fix: on a FAILED boot load, fast-retry with backoff (new Fathom.Tenants.DenyList: 1s→5s→30s cap, config :tenant_denylist_retry_ms/:tenant_denylist_retry_cap_ms) instead of the full interval, emit [:fathom,:tenants,:denylist,:degraded] on each unloaded attempt + paired :recovered on convergence (fathom.tenants.denylist.degraded.count counter, tags: [:kind]). load_from_directory/reconcile now RETURN :ok|{:error} (was always :ok) so init/retry can branch; a successful boot stays on the normal refresh. Storage tombstones (#6, S3-derived) still load during a PG outage (independent). Injectable name/table/loader/retry_ms opts for an isolated test instance. Test env quiets the app-singleton's ownerless boot-load retry (:tenant_denylist_retry_ms 1h). 3 tests (tombstone converge / suspend converge / clean-boot no-degraded); verified they bite (neuter fast-retry→refresh fails the tombstone converge, not the suspend). Reader/admission hot path (tombstoned?/suspended?) unchanged → [skip-bench]. |
| 25 | SKIPPED (owner) | per-tenant request-rate limit — the finding marks it OPTIONAL; fathom is a single company's internal platform, NOT usage-billed ([[fathom-not-usage-billed]]), and noisy-neighbor is already covered by the per-query resource bounds (:query_timeout_ms / :query_max_rows / :max_checkouts_per_shard, review #26) + shard relocation. Owner said skip. |
| 4 | DEFERRED | re-pullable upstream data; optional S3 versioning only |
| 5 | DEFERRED | runbook/IAM note, not code |
| 10 | DEFERRED | per-tenant encryption — overkill for internal-use model |
| 11 | DEFERRED | usage metering — not a usage-billed platform |

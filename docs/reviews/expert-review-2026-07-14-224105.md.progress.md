# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-14-224105.md
**Started:** 2026-07-14
**Last updated:** 2026-07-14
**Strategy chosen:** Lead with the owner's requested + upweighted items — the two UIs (#22 directory browse/edit, #23 query console) and the PITR on-ramp (#13 bucket versioning, #12 restore tooling). Each is a multi-file feature → planned before coding; the rest of the list is deferred to a later pass.

## Lane (this run)

| # | Status | Summary |
|---|--------|---------|
| 23 | FIXED | Front-door Hrana query console: `Fathom.QueryConsole` + `AdminQueryLive` at `/admin/query`; loopback `/v2/pipeline` POST with `Host: <shard>.<base>` routing, admin-auth gated, full R/W (D3), real error code + latency shown; e2e + isolation + LiveView tests |
| 22 | FIXED | Directory browse/edit at `/admin/directory`: `AdminDirectoryLive` (filter by status/id, paginate) + guarded `Directory.admin_update/2` / `Shard.admin_changeset/2` (only `status`/`retain_until` castable — schema_version/token/cutover can't be hand-flipped); context + LiveView tests incl. the edit-guard invariant |
| 13 | FIXED | S3 bucket versioning + hardening documented in `docs/durability.md` (defense-in-depth beneath the fathom-managed snapshots): versioning+lifecycle, Object Lock/MFA-delete, cross-region replication, least-privilege node creds |
| 12 | FIXED | Fathom-managed point-in-time snapshots: Storage `snapshot`/`list_snapshots`/`restore_snapshot`/`drop_snapshot` (both backends, `<shard>@snap-<id>`), `Fathom.Snapshots` context (drain + cross-node lease-holder guard on restore), `mix fathom.snapshot create\|list\|restore\|drop`; Local e2e tests (revert, list/drop, isolation, busy-refusal) + opt-in S3 round-trip. Same copy-one-object primitive is the fork kernel |

## Backlog (deferred — not this run)

| # | Status | Class |
|---|--------|-------|
| 1 | FIXED (partial) | Capture lints the buffer for template-literal DATA migrations (DML on non-django_migrations tables) and, when found, escalates to a loud `Logger.error` + `[:fathom,:migrator,:data_migration_captured]` telemetry — converting silent corruption into an alertable signal. Still RECORDS the version (refusing would fork the template from the fleet, per #19). Follow-up: block rollout via a `requires_review` flag (schema change). Tests: flags a RunPython UPDATE; doesn't flag pure DDL. |
| 2 | FIXED | FK enforcement default-on: `PRAGMA foreign_keys=ON` in `Connection.open/1`, config-gated `:foreign_keys` (default true); regression test pins enforcement-on-by-default + the opt-out branch. Bench healthy (cold_open ~1.67ms). |
| 3 | FIXED | Hrana error-code mapping: `sqlite_code/1` maps exqlite reasons → real codes (SQLITE_CONSTRAINT_UNIQUE/FOREIGNKEY/NOTNULL/CHECK/PRIMARYKEY, SQLITE_BUSY/FULL/READONLY) so Django can classify IntegrityError vs OperationalError; `:busy` now reads "database is locked" not ":busy". Error-branch only (hot success path untouched). Regression tests for UNIQUE + FK codes. |
| 4 | FIXED | `PRAGMA quick_check` gate in `upload_for_drop` before the checkpoint-then-raw-upload (the VACUUM-INTO path already reads every page); on failure quarantines the corrupt local to `.corrupt.<ts>`, refuses the flush (good stored object stays authoritative), releases the lease, alarms (`[:fathom,:shard,:corrupt_flush]`). Detection unit tests + e2e (corrupt local doesn't clobber the good object). Flush-drop path only, not a benched hot path. |
| 5 | FIXED | Self-fence quarantines a dirty local copy (`.fenced.<ts>`, mirroring `quarantine_fork!`) instead of `drop_local`, at both sites (terminate lease_lost + flush_then_drop :superseded); emits `[:fathom,:shard,:fenced_quarantine]` with the un-flushed count. Regression test proves the local survives a steal. Self-fence path only, not a benched hot path. |
| 6 | FIXED (partial) | Backwards Django migrate (django_migrations count falls on commit) now alarms loudly (`Logger.error` + `[:fathom,:migrator,:backwards_migrate]`) instead of silently returning `:noop`. Regression test. Follow-up: non-atomic (`atomic=False`) gap detection — dropped a `:baseline` tracker because it false-alarmed across independent sequences on the shared Capture singleton; needs a non-stateful approach. |
| 7 | FIXED | Config-gated `:block_tenant_ddl` (off by default) refuses client-issued DDL (CREATE/ALTER/DROP) on non-template shards with `FILO_DDL_BLOCKED` — a direct `manage.py migrate` on a tenant fails loudly at the client instead of silently diverging the version stamp and later quarantining. Template exempt; engine replay (uses Connection directly) unaffected. Regression tests: tenant DDL blocked, DML allowed, template exempt, off-by-default allows. |
| 8 | FIXED | `laggard_query/1` excludes `:template_shard_id` — the template (migrated directly by Django, stamp stuck at v0) is no longer swept by laggards/count_laggards/rollout/reconcile, so the engine can't drain+replay its own DDL onto the capture source and quarantine it. Regression test in rollout_test. |
| 9 | PENDING | blocked-on-decision: Filo packaging (publish/public) |
| 10 | FIXED | Fork-from-template new-tenant bootstrap, gated OFF by default (`:fork_from_template`). `Storage.fork_from/3` (cross-shard copy) + `drop_live/1` in both backends; `Migrator.retain_template_head/0` (drain idle template + `retain@HEAD`, cross-node lease guard) + `fork_from_template/1`; `ShardMigration.fork/4` reuses `with_lease`/`Copy.migrate`(user_version stamp)/`fence`/`flush_fenced`/`Directory.cutover` — same stamps a real migration does. Admission hook in `start_if_capacity` (rescue-wrapped, fails open → born empty). `mix fathom.snapshot template-head`. 14 tests incl. the isolation gate (no template data leaks; A/B/C independence), no-clobber (`:dst_exists`), mid-stamp-failure-leaves-no-object, flag-off unchanged. Two documented residual risks (concurrent-first-checkout availability blip; kill-9-mid-fork window). |
| 11 | PENDING | architectural/upstream: Django UDF compat pack (NIF/extension) |
| 14 | FIXED | Operator shard tooling + database forking (`6000ee0`). `mix fathom.shard pull\|inspect\|fork`: `pull` downloads the stored .db; `inspect` pulls + read-only `quick_check`/`user_version`/per-table row counts (a per-shard restore drill — proves a backup is real); `fork` clones a live tenant to a NEW id. Fork (user-upweighted) is the DB-forking kernel — one object copy: `Storage.fork_shard/2` (both backends, refuse `:dst_exists`/`:no_source`) + `Tenants.fork/2` (refuse dst taken/tombstoned, copy, register dst at the SOURCE schema version so the laggard sweep won't re-migrate the fork, mint dst token; non-disruptive to src) + `POST /api/tenants/:id/fork`. Tests: clone independence, schema-version carry, refusals, API contracts. Follow-up: a scheduled fleet restore-drill (sample N/day) — per-shard `inspect` is the manual form. |
| 15 | FIXED | Tenant delete + export. `Storage.purge_shard/1` (both backends, exact id-delimiter match — never touches a sibling id) erases live/lock/versions/snapshots. `Directory.tombstone/1` + a `deleted` status + `Fathom.Tenants.Tombstones` (public ETS re-mint gate: boot-load + Oban-notifier fleet push + periodic refresh; also purges warm-follower copies) make admission fail-closed so a deleted tenant can't resurrect. `Tenants.delete/1` tombstones+broadcasts synchronously then enqueues `DeleteJob` → cancel pending per-shard jobs, `Shards.stop/1` force-stop (fix for the self-fence `.fenced.<ts>` quarantine leak a graceful drain left behind), purge storage + local files. `Tenants.export/1` + BasicAuth `GET /admin/tenants/:id/export` (`send_download`, temp deleted after) = GDPR portability. `AdminDirectoryLive` per-row Delete(confirm)/Export; `deleted` excluded from hand-edit. Tests: isolation gate, re-mint refusal, no-quarantine-leak, export validity, controller/LiveView. Follow-up: cross-node busy-shard drain via command_poller (single-home + fenced-flush self-fence covers correctness today). |
| 16 | PENDING | out-of-repo: fathom-django companion package (Python) |
| 17 | FIXED | Core eval stack + quickstart (fly/terraform deferred by owner). `mix.exs` gains a `releases:` block. `deploy/compose/`: production `Dockerfile` (one image, `WEB_INSECURE_LOCAL` build-arg = eval-plaintext vs prod-SSL), safe-defaults `docker-compose.yml` (fathom+MinIO+Postgres+nginx, `docker compose config` validated), `nginx.conf` (hash-on-Host, Host preserved), `README.md` (zero-to-served-tenant <30min: up → `POST /api/tenants` → query via raw Hrana/SDK). `docs/configuration.md` — every env knob grouped with default + **safety consequence**, kept complete by `test/fathom/configuration_doc_test.exs` (drift test: parses `runtime.exs` incl. the piped `SHARD_BASE_DOMAIN` form, asserts each documented; fails-first if a knob is added undocumented). `docs/quickstart-django.md` — fathom side complete, multi-tenant routing honestly scoped to the pending #16 companion pkg + the proven `test/django_validation` harness + the Python 3.12/Django 5.0.x constraints. Suite 741 (+1). Not a benched hot path. |
| 18 | FIXED (doc) | Multi-worker write contention. Owner call: internal DB, we control the apps, so no server-side SQL rewrite — a client-side one-liner fixes it. `docs/quickstart-django.md` gains a "Write concurrency" section: a multi-worker tenant must set `OPTIONS: {"transaction_mode": "IMMEDIATE"}` (Django 5.1) / an init `BEGIN IMMEDIATE`, or the common `get(); save()` in `transaction.atomic` hits non-retryable `SQLITE_BUSY_SNAPSHOT` (busy_timeout doesn't wait for it). fathom deliberately does NOT rewrite SQL server-side (you control the writes). Docs-only. |
| 19 | FIXED | Per-shard size cap: `PRAGMA max_page_count` at `Connection.open` from `:shard_max_page_count` (pages; unset = unlimited), `SHARD_MAX_PAGE_COUNT` prod env. A runaway tenant's write past the cap fails `SQLITE_FULL`, enforcing the "limited dataset per shard" premise. Regression test (also exercises #3's SQLITE_FULL mapping). Follow-up: per-shard directory-backed quota (this is the fleet-wide default). |
| 20 | FIXED | Suspend/resume tenant state. `suspended` status + `Directory.suspend/1`/`resume/1` (refuse a deleted tenant) + `Fathom.Tenants.Suspensions` — the Tombstones gate but reversible (add/remove notification, periodic insert-then-prune reconcile). `Tenants.suspend/1` flips directory + broadcasts (all nodes deny new streams) + graceful-drains the home coordinator; `resume/1` reverses. Both lifecycle denies moved into `Shards.ensure/1` (every checkout, O(1) ETS) so a deleted/suspended tenant with a running coordinator is denied too (closes a #15 window). `ShardExecutor`: `:shard_suspended`→403 `FILO_TENANT_SUSPENDED`, `:shard_tombstoned`→410 `FILO_TENANT_DELETED` (was status-less 500). Surfaces: `POST /api/tenants/:id/{suspend,resume}` + admin Suspend/Resume buttons. Tests: gate/403/notification, orchestration refusals, API + admin. |
| 21 | FIXED | Provisioning control-plane API. `Tenants.provision/1` (validate → refuse tombstoned/already-exists → explicit directory insert active → fork-from-template@HEAD when `:fork_from_template` on, else born-empty → mint token; returns `%{shard_id, url: libsql://<id>.<base>, auth_token, auth_required}`) + `FathomWeb.Api.TenantController` under `/api` (JSON, same admin BasicAuth, on :4000): POST (201/400/409), GET list (reuses `list_page`), GET :id (200/404), DELETE (202, reuses `Tenants.delete/1`). Tests: provision refusals + full CRUD + auth gate. Follow-up (noted): optionally flip novel admission to known-tenants-only in prod via `:novel_shard_rate`. |
| 24 | FIXED | Token lifecycle. **Zero-downtime rotation** (the "rotation is an outage" headline): a `token_version_bumped_at` column + `Directory.rotate_token/1`; `verify` accepts `version >= floor OR (version == floor-1 AND within :hrana_rotation_grace_ms)` — so `HranaAuth.rotate/1` mints a new token while the old keeps working for the grace window, whereas `revoke/1` (bumped_at=NULL) is immediate. Revocations caches `{floor, bumped_at}` off the hot path. **Read-only scope**: `token_for(id, scope: :ro)` embeds an `"sc"` claim; `authorize/2` stashes the verified scope (same-process) and `ShardExecutor.open` rides it in the handle, so `execute` refuses any write on a `:ro` token with 403 `FILO_READONLY` (no Filo change). **Expiry**: a loud boot warning when `:required` with infinite `max_age`. **API**: `POST /api/tenants/:id/token`(mint, scope), `POST .../token/rotate`, `DELETE .../token`. Three commits (`f46cfd7`/`92494ac`/`df850d1`). Tests: grace window (new+old valid / old dies after grace / revoke immediate / only floor-1 covered), ro read-ok-write-403, API contracts, boot warning. |
| 25 | PENDING | docs+code: Django migration workflow + convergence API |
| 26 | FIXED | Per-query resource bounds (`3eafa12`). (a) `:query_timeout_ms` — `Connection.query` runs with a watchdog process that `Sqlite3.interrupt/1`s the conn at the deadline (503 `FILO_QUERY_TIMEOUT`); only armed when set. (b) `:query_max_rows` — `collect/2` errors past the cap instead of materializing an unbounded result (400 `FILO_RESULT_TOO_LARGE`); also fixed a latent stmt-leak (`do_query` now releases in an after-block on every path). (c) `:max_checkouts_per_shard` — the coordinator refuses a checkout past the cap (503 `FILO_SHARD_BUSY`) so one tenant can't monopolize a node's streams. All config-gated, off by default. Tests: each bound trips + passes through. |
| 27 | FIXED (core) | RPO telemetry + doc correction (per-shard override scoped OUT — it's the tenant-tiering feature dropped in #36). A persistent flush failure was only a per-interval `Logger.warning` → silent unbounded RPO. Now `Fathom.Shard` tracks consecutive flush failures in state, emits `[:fathom,:shard,:flush,:failed]{consecutive}` telemetry each time, escalates to `Logger.error` past `:flush_failure_alert_threshold` (default 3), and RESETS on any durable flush. Exported as `fathom.shard.flush.failed.count` + a `FathomFlushFailing` page rule in `deploy/observability/alert-rules.yml`. `docs/durability.md` corrected: worst-case RPO is `interval + ~2× flush duration` and UNBOUNDED under persistent S3 failure (now alertable). Regression test: 2 consecutive failures → escalating telemetry + Logger.error, then reset-on-recovery, then a fresh failure restarts at 1. Coordinator change → bench-gated. Suite 742 (+1). |
| 28 | FIXED | Post-node-loss loss accounting (`363a49a`). `last_flushed_at` on the directory (the FlushWatermark was node-local ETS that dies with the node). `Directory.record_flush_batch/1` (GREATEST, never rewinds; leaves last_active_at alone) fed by a parallel `Directory.Recorder` flush-time buffer (`record_flush/1`, same coalesce/batch shape, off the coordinator's flush hot path); `Fathom.Shard` hooks it at every successful upload. `Directory.flush_lag_report/1` + `mix fathom.shard loss-report` = the report: shards with `last_flushed_at` NULL or `last_active_at > last_flushed_at` (active-since-flush ⇒ potentially lost), with the per-tenant window, excluding deleted. Tests: set/advance/never-rewind + report inclusion/exclusion + Recorder batch-write. |
| 29 | FIXED | `docs/runbooks/operations.md` — the non-lease incident classes: Postgres-outage (fail-open, weakened novel-limiter security note, Oban stall), node disk-full (`SHARD_DATA_DIR`/warm-cache consumer triage + relief), heartbeat-object loss (mass-stealable, fence still holds, `node_key` collision, lifecycle-exclude the `heartbeat/` prefix), dual-LB double-routing (churn-not-corruption, reconcile to one config), and the shard restore drill (`mix fathom.shard inspect\|pull\|loss-report`, export; no PITR). Leads with a per-dependency **fail-open/fail-closed matrix** (S3 closed, Postgres open, LB availability-only). Cross-linked from `cluster.md` + `docs/README.md`. Docs-only. |
| 30 | FIXED | Observability PACKAGED, not just exported. `deploy/observability/`: `prometheus.yml` (scrape `/admin/metrics` BasicAuth, relabel target→`node`, fleet-merge by `node`), `alert-rules.yml` (15 rules / 5 groups, each linking its runbook), `fathom-dashboard.json` (Grafana: node health, RPO exposure, lease/heartbeat churn, capacity/admission, S3+Oban), `README.md` with 3 SLOs (checkout success ≥99.9%, cold-open p99, unflushed-age ceiling) + PromQL. Also EXPORTED the page-worthy signals that emitted telemetry but weren't in `metrics/0`: `corrupt_flush`, `fenced_quarantine`, `novel_rate_limited` (429s), `evicted`, `heartbeat.lapsed` (mass-self-fence precursor), `oban.job.exception{queue}`. Regression test pins every alert-rule metric stays exported (fail-first) + the Oban metric's queue tag/event wiring. Exporter naming verified from the dep (underscore-join, no `_total`). Not a benched hot path (metrics/0 = supervisor init). |
| 31 | FIXED | Deploy runbook + release discipline + LIVE clean-shutdown verification. `docs/runbooks/deploy.md`: node-by-node rolling upgrade, what tenants experience (reroute + survivor cold-open + immediate lease acquire since clean shutdown RELEASES the lease — no TTL wait), `:shard_shutdown_ms` sizing formula, config-change vs restart, node-removal drain (the known-manual gap), fathom↔Filo compat. **Verified LIVE** on the chaos rig (new `chaos.sh deploy` scenario — the herd analog of `failover`): held 200 then 500 shards OPEN+DIRTY (idle+periodic flush disabled so only the terminate path can persist), SIGTERM, verified every committed row survived via a survivor cold-open — **0 loss at both**, graceful stop 1.5s/1.7s (sub-linear ⇒ concurrent flush; `docs/reviews/deploy-clean-shutdown-2026-07-16.md`). `CHANGELOG.md` started (Keep-a-Changelog + a fathom↔Filo version-pair table), `mix.exs` 0.1.0→0.2.0 (tag left to the owner). Docs + a host bash scenario + version string — no benched hot path. |
| 32 | FIXED | Revert's Django-side story + a consistency check. `docs/migration.md` gains the full revert loop (yank+fleet-revert → **backwards-migrate the template** → author the fix → re-release) — the template gotcha (linear graph left ahead of HEAD → next makemigrations forks the fleet, #6). Consistency check `Migrator.template_drift/0`: the template's captured `django_migrations` count is now persisted per release (`shard_migrations.template_migration_count`, additive nullable migration; Capture threads the count it already computes), and the check compares the highest yanked-above-HEAD version's count to HEAD's — `{:drift,_}` when the template's left ahead, `:unknown` for pre-feature (nil) releases (no false alarm), `:aligned` otherwise. `yank/1` runs it, emitting `[:fathom,:migrator,:template_drift]` telemetry + a loud Logger.error naming the backwards-migrate fix. Robust: stored-count comparison, no template I/O, no SQL-name parsing (params aren't in captured SQL — the #6 fragility trap avoided). 3 regression tests (aligned/drift+telemetry+log/unknown). Off the copy hot path. |
| 33 | PENDING | cross-repo (Filo): stream idle_timeout config |
| 34 | FIXED | Hrana `sequence` + `describe` (`11d0547`). `execute_sequence/2` runs a script via `Connection.exec/2` and — the durability trap the fix note flags — bumps `WriteCounter` UNCONDITIONALLY (exec bypasses the `wrote?` bump, so a script-only session would leave the shard clean and drop its writes on the next idle flush); honors the ro scope (#24) + DDL block (#7). `describe/2` via `Connection.describe/2` (prepare + columns + `bind_parameter_count`) reports cols/param-count/is_explain/is_readonly. So `executescript()` and prepare-introspection work. Regression pins the durability trap (a script's writes survive drain+reopen) + describe of SELECT/write/EXPLAIN + ro-refuses-script. |
| 35 | FIXED | `autocommit?/1` reports real state via `Connection.autocommit?/1` (exqlite `transaction_status`) instead of hardcoded true — so libSQL batch `is_autocommit`/`get_autocommit` conds don't skip a guarded COMMIT/ROLLBACK and leave a dangling transaction on the WAL write lock. Regression test: true→false→true across BEGIN/COMMIT. |
| 36 | WON'T DO | Per-tenant usage metering. Owner call (2026-07-16): fathom is an INTERNAL database, not a billing product — the point is not to charge users. Live per-tenant cost attribution already exists (`Fathom.ShardLoad` + `mix fathom.scale --hotspots` + the rebalancer); durable monthly invoice-grade rollups are the specific part an internal deployment doesn't need. |
| 37 | PENDING | test-infra: Django CI harness (needs Python venv) |
| 38 | FIXED | Django latency contract in `docs/quickstart-django.md` ("Request latency & keeping tenants warm"): the two regimes (warm = local SQLite sub-ms; cold = ≈1 S3 RTT + body transfer, inline in the first query), the p99 contract from the fleet numbers (24/77/137 ms at 10/30/60 ms one-way RTT — ~2× one-way + a few ms, priced not pathological), and three levers — `CONN_MAX_AGE=None` (default 0 churns a stream/request), `SHARD_IDLE_MS` tuning, and an optional pre-warm-on-login (warm follower covers failover, not first-touch). Docs-only. |
| 39 | PENDING | ops+docs: failover-herd chaos scenario + sizing |
| 40 | PENDING | code+docs: enqueue-on-touch migrate mode |
| 41 | FIXED | Tests pin "every flushed object is a valid, quick_check-clean db with the right rows" for the two default-suite flush paths (checkpoint+raw-upload on idle-drop, VACUUM-INTO on periodic flush) via `assert_stored_object_valid/2`. Test-only. (S3 multipart path left for an opt-in follow-up.) |
| 42 | FIXED | `to_stmt_result` classifies read-only as `cols != [] and not dml?(sql)` (leading-keyword), so `INSERT/UPDATE/DELETE ... RETURNING` reports real `affected_row_count`/`last_insert_rowid`/`rows_written` instead of 0/nil/0. Cheap 7-char keyword check on the hot path (no bench metric measures it). Regression tests: INSERT/UPDATE RETURNING + SELECT-still-0. |

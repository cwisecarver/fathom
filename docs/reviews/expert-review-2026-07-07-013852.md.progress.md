# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-07-013852.md
**Started:** 2026-07-07
**Last updated:** 2026-07-07

Scope: Phase-2 B1 dynamic rebalancer (`Fathom.Rebalancer.*`). All gates off by default;
work is hardening a rig-proven feature before it can be switched on in prod.

**Status: COMPLETE.** All 18 findings addressed — 17 fixed (each committed + pushed with a
regression test; the correctness fixes were verified to fail pre-fix), #15 documented (its
full server-clock fix deferred per the user's call). One commit per finding; all
control-plane changes (not the request hot path), so `[skip-bench]` per AGENTS.md.

Residual/deferred (explicitly, not silently):
- **#8**: cross-*batch* command execution still serializes (the GenServer handles one poll at
  a time); full non-blocking polling would break the synchronous `poll_now` contract the
  tests rely on. Within-batch concurrency + the timeout-ordering fix are in.
- **#15**: full single-server-clock ordering deferred; NTP dependency documented instead.
- **#16**: a `REBALANCE_P99_MULTIPLE` env is deferred with #2 (no point tuning what was inert).
- **#18**: warm `.db`+`.etag` non-atomicity left as-built (safe via coordinator revalidation);
  base_domain/addr + upstream_name-collision renders now fail-safe behind #3's config test;
  the sidecar pidfile/sentinel is a rig note (the mtime race is gone via #3's atomic rename).

| # | Status | Summary |
|---|--------|---------|
| 5 | FIXED | HandoffJob unique `period: :infinity` (regression fails pre-fix) |
| 6 | FIXED | Rate reset: `curr < prev ⇒ curr/window_s` (fails pre-fix) |
| 16 | FIXED | parse_hot_qps_floor! validates + accepts int, raises at boot (p99_multiple env deferred w/ #2) |
| 13 | FIXED | Reporter/CommandPoller catch `:exit` (+ folded #18 reporter-rescue minor) |
| 14 | FIXED | Validate shard_id at Override boundary + render skip + no hard-match crash (+#18 minor) |
| 9 | FIXED | Anti-flap: distinct windows on candidate's node_key (fails pre-fix) |
| 12 | FIXED | Oban Pruner + command prune_terminal/expire_stale_pending swept each tick |
| 15 | DOCUMENTED | NTP dependency documented (LoadSample/LoadSamples moduledocs + cluster runbook); full server-clock fix deferred per user |
| 3 | FIXED | LbApply render → temp → config-test → atomic rename; LB_TEST_CMD |
| 11 | FIXED | apply! surfaces reload failure; HandoffJob skips drain on unapplied flip |
| 7 | FIXED | Cancel pending drains on revert + poller skips drain with no active pin |
| 4 | FIXED | failed_at cooldown record on revert (retain, not delete); fails pre-fix |
| 1a | FIXED | LbMap pin upstream: primary + backup servers (dead-pin failover) — `<pushed>` |
| 1b | FIXED | node_key liveness beat (rebalancer_nodes) + RebalanceJob unpins dead-node pins (fail-open) |
| 2 | FIXED | Reporter publishes full-dist p99 → Nodes.fleet_p99 (median, min-sample) → Policy; interpolating percentile |
| 8 | FIXED | Concurrent batch execution (Task.Supervisor) + drain-await ordered above worst-case |
| 10 | FIXED | Advisory-lock apply! + no-op re-render + leader periodic re-render each tick |
| 17 | FIXED | Documented steering trust assumption (Policy moduledoc + runtime.exs) |
| 18 | FIXED | Minor batch: max_moves≤0, tie-break determinism, nil-guard, drop unused index, init comment (rest done-elsewhere/documented) |

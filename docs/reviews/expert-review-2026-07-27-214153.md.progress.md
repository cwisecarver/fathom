# Iteration Progress

**Source:** `docs/reviews/expert-review-2026-07-27-214153.md`
**Started:** 2026-07-27
**Last updated:** 2026-07-27 (26 of 36 fixed. Remaining: 9 GATED on operator approval + #24, partially covered)

Findings span three repos. Commits land in the repo that owns the file:
fathom (this one), `../djathom`, `../django-fathom-example`.

## Operator decisions (carried across the whole run)

- **Ports stay on `0.0.0.0`.** The user runs the demo reachable from other devices
  on the LAN on purpose. #1's loopback-bind recommendation was **declined**; the
  credential-hygiene half of #1 was done instead, which is what makes the open
  bind safe to keep. Do not re-propose loopback binding.

## Status

| # | Status | Repo | Summary |
|---|--------|------|---------|
| 1 | FIXED | djathom | Renamed `FATHOM_ADMIN_AUTH` → `FATHOM_ADMIN_SECRET` so Django's debug filter cleanses it (verified against Django 5.0.6's regex: the old name printed the password in plaintext on any 500 page); made both compose files interpolate `${FATHOM_ADMIN_PASS:-admin}` so the already-env-driven settings can actually be overridden. Ports left on 0.0.0.0 per operator decision. Regression test `tests/test_settings_hygiene.py` (4 tests; 3 fail pre-fix). djathom `dbb1cdc` |
| 2 | FIXED | djathom | Pitch now states the measured RPO (bounded window, p99 ~= flush interval) instead of "without losing committed data"; Act 8 and the close already said it correctly, so the file no longer contradicts itself. djathom `65edf78` |
| 3 | FIXED | fathom | docs/README.md: added the 11 missing 07-18->07-25 reports, corrected flush ~3.5 -> ~2.3x RTT, marked the 07-11 entries superseded, and bannered tpc-fleet-2026-07-11.md itself (its absolutes were ~95% LB-502 artifact; current is 4,096 tenants @ 3,414 txn/s). All links verified to resolve. fathom `8120d50` |
| 4 | FIXED | djathom | DEMO.md gained a Prerequisites section (Docker + Compose v2, the three-sibling clone layout, clone commands verified against the real remotes) above the one command that needs them. djathom `f6bbca8` |
| 5 | FIXED | djathom | "TLS end to end" -> client wire is TLS, LB terminates, LB->node is plaintext on the private segment. Verified: nginx-tls.conf does `proxy_pass http://` on both listeners. Fixed in DEMO.md + settings.py comment. djathom `a944270` |
| 6 | FIXED | djathom | Blast-radius claim corrected to no-offline-forgery + audited/revocable mint path; the capability-vs-tenant scope gap stated as a next step. Verified against api_keys.ex:20, router.ex:66/106. Act 3's Proves softened to the connection-scoped (FILO_READONLY) claim. djathom `a944270` |
| 7 | FIXED | djathom | Act 5 delete guardrail: DEMO.md warning + fork-first ordering, AND delete_home refuses settings.FATHOM_PROTECTED_HOMES (default home-00001) with a 403 before reaching the control plane; env-overridable. 4 regression tests. djathom `90ef2bc` |
| 8 | FIXED | djathom | Documented the /etc/hosts step for `*.fathom.local` (CoreDNS only serves the compose network) and named the failure mode as DNS-not-TLS; gave the zero-setup in-network alternative. djathom `5fa8d4b` |
| 9 | FIXED | djathom | Pitch + close now cite the measured 30,000 across 3 nodes @ ~16 KiB, spread 1.22x, and frame millions as nodes x measured ceiling. djathom `2397b79` |
| 10 | FIXED | djathom | Act 2 now shows the FILES (MinIO one-object-per-tenant, or two exported .db opened in sqlite3); the 404 is kept but demoted to the app-layer half, since a tenant_id app produces the same 404. djathom `2397b79` |
| 11 | FIXED | fathom | Warm-standby 2.3x now carries its date + 100 Mbps cap, and states the uncapped 07-23 A/B where cold/warm converge (607/619 ms). Fixed in README + docs/warm-standby.md. fathom `3e99bc5` |
| 12 | PENDING (GATED) | both python | Alias registry mutated with no lock |
| 13 | FIXED | djathom | Added a what-proves-what row citing django-fathom-example's churn (20k tenants / 1,000-shard cap / 1,977 verified cold reopens / 0 refusals) + a presenter note to name the 8-home gap rather than let it be found. djathom `2397b79` |
| 14 | FIXED | djathom | add_transaction now classifies the exception: only FILO_READONLY renders the :ro proof, anything else names its own cause. test_ro_scope tightened to assertRaisesRegex. 4 CI-runnable regression tests (3 fail pre-fix). djathom `8c99cba` |
| 15 | PENDING (GATED) | djathom | Scope ContextVar defaults `rw`, never reset |
| 16 | FIXED | djathom | Token cache LRU-bounded (FATHOM_MAX_ALIASES) + 15-min TTL (inside fathom's 1h rotation grace) + invalidate_token() hook, so rotate/revoke propagate instead of waiting for a process restart. 7 tests (2 fail pre-fix). djathom `8c99cba` |
| 17 | PENDING (GATED) | djathom | DNS-safe shard ids not enforced at the boundary |
| 18 | PENDING (GATED) | djathom | CI green, never runs cross-shard isolation test |
| 19 | FIXED | djathom | failover.sh now asserts PASS/FAIL per claim and exits non-zero; a lost FLUSHED write is called a durability violation, while volatile-B surviving is NOTE not FAIL (the window is a bound, not a promise). djathom `3adace5` |
| 20 | FIXED | djathom | durability_proof.md flush default 30s -> 5s (+ the measured sweep); failover_proof.md's 15.6s tagged as rig-tuned with the production floor (~35s + cold-open) stated. Defaults verified in shard.ex:70/74, storage.ex:543. djathom `3adace5` |
| 21 | FIXED | fathom | djathom linked from README Examples (first, framed as "start here"), docs/README.md "New here?", and the quickstart intro; example repo given a distinct role. fathom `7746f5a` |
| 22 | PENDING (GATED) | fathom | README opens with plumbing |
| 23 | PENDING (GATED) | fathom | Proof points buried |
| 24 | PARTIAL | djathom | `tests/test_protected_homes.py` (from #7) added the first Django test-client coverage of a destructive view — it drives `delete_home` and asserts the guard. Still uncovered: the staff-gate 404 for a non-staff user, POST-only (405) enforcement, and the other four compliance actions (export/suspend/resume/fork). |
| 25 | FIXED | example | Fork-from-template and in-app bearer tokens were documented as unbuilt; both ship. Bullets now say "this example's choice, not a fathom limitation" + dated the Sharp-edges header. example `293e0ec` |
| 26 | FIXED | djathom | The `:ro` test's prereq is now a runnable command pair (HRANA_AUTH=required alongside the test), with the reason auth-off makes it meaningless. djathom `eaa4cfe` |
| 27 | FIXED | djathom + example | `mix ecto.migrate` -> `mix setup` for a first run (verified mix.exs:114 -> ecto.create); Postgres server named explicitly. example `293e0ec`, djathom `eaa4cfe` |
| 28 | FIXED | djathom | MinIO console port 9001 actually published (it never was); doubles as the #10 evidence path. Verified with `docker compose config`. djathom `5fa8d4b` |
| 29 | FIXED | fathom | quickstart re-cited to latency-cost-2026-07-23 (26/70/133 ms) instead of the report whose flush analysis was retracted. fathom `1ea7ea2` |
| 30 | FIXED | djathom | Removed the `~/.gstack/...` private path from a README heading; relative sibling link -> GitHub URL. djathom `eaa4cfe` |
| 31 | PENDING (GATED) | fathom | Architecture diagram buried |
| 32 | PENDING (GATED) | fathom | "What's built" is a module inventory |
| 33 | FIXED | djathom | "vendored verbatim" corrected: 4 of 5 files byte-identical (verified), aliases.py diverged 37 lines for the scope hook + token provider. djathom `eaa4cfe` |
| 34 | PENDING (GATED) | djathom | Demo serves via `runserver` |
| 35 | FIXED | fathom | "under 30 minutes" (a stated goal, never timed) -> "a single sitting" + the first-build caveat. fathom `1ea7ea2` |
| 36 | FIXED | fathom | turso-headtohead + competitive-oltp added to further reading, labeled by the question they answer ("why not Turso", "why not Postgres"). fathom `1ea7ea2` |

GATED = flagged for approval before implementing (architectural, multi-file, or
rewrites the public README).

# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-23-001259.md
**Started:** 2026-07-23
**Last updated:** 2026-07-23 — COMPLETE (all 31 findings resolved: 24 fixed/partial-fixed, 7 carry explicit deferred/skipped rationale)

Fathom commits: 7f908c8 → b50007c (this repo). Filo commits: a395b0c, 4e2dfc3, 232bbb1 (../filo).
Every hot-path fathom commit went through the bench gate (worst observed delta +12.9% on
dir_resolve, a metric that oscillated ±13% across all runs — noise band; nothing approached
the 20% block threshold).

| # | Status | Summary |
|---|--------|---------|
| 1 | FIXED | 90e9c29 — per-connection LRU-64 prepared-statement cache (reset between executions); invariants + @tag :bench floor pinned |
| 2 | FIXED | 7f908c8 — slice-first downcase in ends_transaction?/control_statement?; full downcase only for pragma bodies |
| 3 | FIXED | 4b94954 — :otel_spans defaults off; runtime.exs enables it with the exporter; default pinned by test |
| 4 | FIXED | filo 4e2dfc3 — JSON transports get rows as ONE flattened-binary Jason.Fragment built in the stream/socket process; the HTTP reply crosses heaps as a refc-binary reference, and Jason splices instead of re-walking |
| 5 | FIXED | filo a395b0c — Value.encode_json direct-to-iodata cell encoder; wire equivalence pinned across the full value matrix; protobuf keeps :maps |
| 6 | FIXED | df1ac8a — lease carries lock_etag; release = 1 conditional DELETE, renew = 1 conditional PUT (drain 3→2 RTTs — the AGENTS.md flush tuning target); request shapes pinned by test; also corrects the latency-cost doc's sidecar attribution |
| 7 | FIXED | 0970a3d (collect cap once/query) + e27e254 (per-stream opts, single :shard_load check). Deliberately kept: migrate_on_touch's legacy double-read (tests flip :lazy_migrate at runtime); Lru pair already short-circuits on the default path |
| 8 | PARTIAL/DEFERRED | Perf half fixed in filo a395b0c (String.valid? pre-scan gone on JSON paths — Jason's own scan is the single pass). The blob-mis-tagged-as-text wire bug and the map-path double scan need tagged-blob reads upstream in exqlite (its NIF returns bare binaries for both TEXT and BLOB); until then behavior is unchanged by design |
| 9 | PARTIAL | filo 4e2dfc3 — encode_to_iodata! at send_resp/WS-frame/cursor sites (final flatten gone). stdlib-JSON migration DEFERRED: Jason.Fragment is now load-bearing for the row fast path (no stdlib equivalent), and rows — the dominant cost — no longer go through the generic encoder at all, so the remaining lib-swap win is far below the panel's estimate |
| 10 | FIXED | Multiplier collapsed to ~2–3× by filo #4/#5/#9; documented in docs/data-path.md where :query_max_rows sizing guidance now lives |
| 11 | FIXED | 095bba4 — ShardRegistry partitioned across schedulers |
| 12 | FIXED | 0970a3d — explicit 500-row multi_step chunk; cap enforcement now exact incl. the final batch (chunk-size-independent) |
| 13 | FIXED | ca488f3 — 1s per-owner heartbeat memo (HeartbeatCache; errors never cached; TTL ≪ 5s steal margin) + confirm-HEAD dropped when the write response proves rotation (HEAD fallback kept; the boot fence probe keeps its own HEAD by design) |
| 14 | FIXED | bd4a39f — two-table LRU (set authority + ordered_set walk), O(probes) per at-capacity admission; one-order-key-per-shard pinned |
| 15 | FIXED | b2bc329 — revalidation rides directory last_flushed_at (GETs track flushes-since-last-poll, not cache size) + rolling 1/10 force sweep + lazy temp file on 304s; fail-pre-fix FaultyStorage test |
| 16 | FIXED | 0970a3d — collect counts once, returns {:ok, rows, count}; rows_read uses it |
| 17 | FIXED | ce7d1d1 — coalesced idle deadline (no cancel/rearm pair per stream cycle); LRU recency single-writer (coordinator release stamp) |
| 18 | SKIPPED | mkdir_p + WAL pragma are load-bearing: the first connection open CREATES a brand-new shard's file (formalized by #19), so skipping journal_mode=WAL would mint non-WAL shards; ~20 non-coordinator callers rely on the mkdir. Residual ~µs/stream; the connection-pool alternative stays measure-first (needs hrana_rt_us wired) |
| 19 | FIXED | acfce4e — drop_clean releases the lease without a local file; fail-pre-fix regression test |
| 20 | PARTIAL | filo 232bbb1 — cursor entries batched into ~32KiB transport chunks (was one chunk/2 + HTTP framing per ~40-byte row). Long-term half (streaming executor callback for O(batch) cursor memory) deferred — a Filo.Executor behaviour extension, its own initiative |
| 21 | FIXED | e27e254 — template?/block_ddl? resolved once per stream in handle opts; ShardId.valid? allocation-free byte walk |
| 22 | FIXED | 387df62 — fork-check HEAD overlapped with lease acquire (warm restart 3→2 RTTs); every quarantine/adopt branch unchanged |
| 23 | PARTIAL/DEFERRED | Panel's minimum (skip quick_check before the snapshot fallback) was already the case — quick_check only guards the raw fast path, a deliberate 2026-07-14 #4 corruption gate. Single-pass trailer-checksum upload is a cross-store (MinIO/R2/Tigris) compat redesign — its own initiative with chaos-rig verification |
| 24 | FIXED | ffe2e7f — recorder buffers integer µs stamps; DateTime built once per distinct shard per flush |
| 25 | FIXED | 77e8ce1 — base Req struct cached in persistent_term keyed by {config, finch?} |
| 26 | FIXED | b50007c — one long-lived deadline watchdog per connection, armed per query; stale-interrupt guarantee preserved via the blocking late-done consume; lifecycle regression test |
| 27 | FIXED | 7f908c8 — statements classified once in do_execute, booleans threaded through (with #2) |
| 28 | FIXED | c7bff9a — novelty computed at most once per cold open, fed to both gates |
| 29 | FIXED | cb9de15 — Reporter keeps prev as raw {co,q,rr} tuples via ShardLoad.snapshot_tuples; single positive-rate reduce |
| 30 | FIXED | c7bff9a — durability O(open-shards) walk gated behind Admin.enabled? (rode along in c7bff9a) |
| 31 | DEFERRED | The report itself says note-and-defer: the single DynamicSupervisor is headroom (S3-bound today), only visible after #11 — re-measure the spawn ceiling with --ramp before considering PartitionSupervisor |

## Live-verification levers (the in-process suite can't measure WAN RTTs)

- `deploy/chaos/chaos.sh latency-cost` re-run: expect drain ~215→~150 ms at 30 ms one-way (#6),
  warm-restart opens ~1 RTT shorter (#22), takeover chains ~2 RTTs + N−1 heartbeat GETs shorter (#13).
- `mix fathom.scale --hotspots` at L=1: the per-stream trims (#17, #21, #2/#27) target the
  hottest-shard stream-admission ceiling; #1's statement cache targets per-query throughput at all L.
- `--ramp` at high N: #11 (registry partitions) — also the precondition for revisiting #31.

# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-09-000847.md
**Started:** 2026-07-09
**Last updated:** 2026-07-09

Scope: this session's rebalancer code (C affinity, fleet-p99 refinement, #1b/#2/#8/#10
architectural batch, observability, bench lock). All B1 gates off by default. Order:
the two High findings first, then the review's recommended fix order.

| # | Status | Summary |
|---|--------|---------|
| 1 | FIXED | Storage.lease_holder S3-gate on reconciler unpin + policy excludes silent targets + NODE_KEY boot-guard + divergence metric |
| 2 | FIXED | Reload moved outside the lock + hard shell timeouts + pg_try_advisory_lock (skip on contention) |
| 3 | FIXED | Affinity ceiling anchored to least + band×q (not from_load); counterexample test |
| 9 | DOCUMENTED | Destructive path closed by #1's S3 gate; NTP residual documented (like #15) |
| 4 | FIXED (ARCH, user-gated → pooled histogram) | fleet_p99 now the TRUE pooled p99 via merged per-node q/s histograms (Stats.histogram/percentile_from_histogram + q_hist column); Policy kills the head-only fallback in orchestrator mode |
| 5 | FIXED | beat/1 stat-preserving upsert (fails pre-fix) |
| 7 | FIXED | drain/2 supersedes a prior attempt's pending drain (single in-flight); + revert cancels + poller re-checks pin (defense-in-depth) |
| 8 | CORRECTED | Premise disproven (telemetry isolates handler crashes); reordered emit-after for consistency + pinned invariant |
| 6 | FIXED | ShardId gate at Command.changeset + WarmLocations.publish filter + WarmFollower.warm_now/cache_path guard; warm/1 tolerates rejected issue |
| 11 | FIXED | Timer path dispatches detached batch + reschedules immediately (in-flight set prevents re-select); finite task timeout makes on_timeout live; poll_now stays sync |
| 12 | FIXED | WarmFollower.cached_shard_ids (one readdir) replaces N File.exists? in the reporter tick; report_now given a 60s call timeout |
| 13 | FIXED | Migration drops unused [:shard_id] index, adds [:updated_at] (serves warm_nodes/prune/sweep); reversible |
| 14 | FIXED | publish upserts-then-age-sweeps (updated_at < now, constant 2-param) instead of unbounded shard_id NOT IN |
| 15 | FIXED | Override.changeset cast+put_change (canonical store) + Overrides API canon/1 on pin/unpin/mark_failed/for_shard (case-insensitive, upsert-safe) |
| 10 | ACCEPTED | Bench lock "sibling-project" name — user-approved this session; recorded as an APPROVED exception in the scrub memory + flagged as a pre-open-sourcing re-scrub item. No code change. |

## Remaining

**None.** All 15 findings are resolved. #4 was gated for an approval decision; the user chose
the **pooled-p99-via-histograms** combiner (the faithful fix), now shipped. Everything else in
the review is FIXED, DOCUMENTED, CORRECTED, or ACCEPTED. B1 is ready for the enable-path
runbook (`docs/runbooks/rebalancer.md`).

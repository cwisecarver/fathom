# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-18-015311.md
**Scope:** findings #8, #10, #11, #12, #14, #16 (the clean mechanical follow-ups)
**Started:** 2026-07-19
**Last updated:** 2026-07-19

| # | Status | Summary |
|---|--------|---------|
| #8 | FIXED | `:skip` flush routed through `record_flush_failure/2` (`:ownership_unconfirmed`) — `fba41be` |
| #10 | FIXED | `requires_review` gated in `Migrator.statements/1` (mirrors `yanked`) — `933cd30` |
| #11 | FIXED | `Directory.touch_migrating/1` renewed by the migration lease renewer — `d3b47bd` |
| #12 | FIXED | `count_at_version/1` + keyset `stream_ids_at_version/2`; revert path streamed — `6b2612e` |
| #14 | FIXED | Fork holds the dst lease + atomic `Local.fork_shard` — `5400b3e` |
| #16 | FIXED | `ShardId.valid?` gate in `AdminQueryLive` before the query runs — `1667fa0` |

**All six landed 2026-07-19**, one commit each, deterministic regression test + `mix precommit` green
(786 tests), all `[skip-bench]` (none touch a benched hot path — verified: cold_open benches
`Shards.checkout`, dir_resolve benches `resolve/1`, copy benches `Copy.migrate_chain`).

## Flagged findings — decisions taken 2026-07-19, all landed

The five FLAG findings, each decided with the user then implemented (one commit each, regression
test + `mix precommit` green, all `[skip-bench]`):

| # | Decision | Summary |
|---|----------|---------|
| #19 | configurable knob, keep 100 default | `:reconcile_batch_size` (`RECONCILE_BATCH_SIZE`) sizes the reconcile sweep — `d56ab05` |
| #6 | loud-shutdown guard (not disk-persist) | `Capture.pending_count/1` + trap-exit + loud `terminate/2` alarm — `600b8fe` |
| #5 | transactional outbox | retirement enqueued atomically with cutover (`cutover_with_retirement/3`) — `a3d6223` |
| #18 | do the refactor | the periodic-flush fence runs off-process in the flush task (`fenced_flush/2`) — `0563d10` |
| #20 | add a cost benchmark, keep 5s | `[:fathom,:shard,:flush]` telemetry + `mix fathom.rpo --cost` (5s = 6× the VACUUM/PUT rate of 30s at ~const per-flush cost) — `f25b625` |

Nothing left open from the 2026-07-18 audit.

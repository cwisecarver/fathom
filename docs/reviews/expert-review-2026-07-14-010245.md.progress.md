# Iteration Progress

**Source:** docs/reviews/expert-review-2026-07-14-010245.md
**Started:** 2026-07-14
**Last updated:** 2026-07-14

| # | Status | Summary |
|---|--------|---------|
| 1 | FIXED | Bump WriteCounter on COMMIT/END boundary. Headline (routine loss) refuted empirically — COMMIT inherits changes>0 in the common case; but a genuine residual (0-row write before a mid-txn flush → COMMIT inherits changes=0 → clean → drop_clean loses the committed row) reproduced deterministically (pre-fix re-open returns []) and closed. |
| 2 | FIXED | Cold-open now O(1): reap only the deterministic `.pull` family by direct name; a gated `TempReaper` GenServer sweeps unique-suffix orphans amortized off the hot path (default-on, off in test). Removed the per-open glob readdir + its GC. |
| 3 | FIXED | check_local_storage_fleet! raises in prod when Local backend + fleet (:lb_backends). [boot guard batch]
| 4 | FIXED | restore/3 is etag-conditional (behaviour+S3+Local); do_revert threads the pull-time live etag so a mid-window steal aborts with :superseded instead of clobbering. Revert-fence test fails pre-fix, passes post-fix; forward+happy+cross-version green.
| 5 | FIXED | promote_pull writes the .etag sidecar BEFORE File.rename — a crash can only orphan a sidecar (harmless), never leave an authoritative .db with no provenance (which would adopt-current and clobber a newer lineage). Post-fix invariant pinned.
| 6 | FIXED | check_shard_base_domain! raises in prod when zone unset + exposed plane, unless ALLOW_UNANCHORED_ROUTING ack. [boot guard batch]
| 7 | FIXED | Admission-path eviction bounded by :evict_budget_ms (default 2s): evict/2 waits at most the remaining budget for the coordinator exit (shared await_coordinator_exit/3 keeps #41 cleanup identical), 503s fast under slow S3 instead of blocking up to 16×30s. Coordinator terminate/flush/drop/release untouched.
| 8 | PENDING | Sync S3 round-trips in handle_info (412/lapse paths) |
| 9 | FIXED | renew_loop continues on transient {:error,_}, stops only on :superseded (renew_continue?/1). Mirrors coordinator.
| 10 | FIXED | Reconciler skips shards with in-flight HandoffJob (Oban query) + from_node-scoped pin-recency grace — no mid-handoff unpin.
| 11 | FIXED | scrape_step holds diff baselines on a blank scrape (no false recovery spike).
| 12 | FIXED | Per-shard percentiles now WINDOWED vs prev tick (diff histograms, per-bucket reset clamp) — matches node-wide windowed p99.
| 13 | FIXED | handle_info folds each metric into @history (append, bounded 300) so KPI sparklines update live.
| 14 | FIXED | Coordinator force-dirties the shard on an ABNORMAL connection :DOWN (crash between commit and WriteCounter.bump) so an idle drop flushes rather than drop_cleaning an uncounted committed write. Zero per-statement cost (chosen over the finding's per-statement pre-bump, which would tax reads/re-dirty setup pragmas). Fails pre-fix.
| 15 | FIXED | check_shard_override! raises in prod when :allow_shard_override on. [boot guard batch]
| 16 | FIXED | check_default_shard! warns in prod on non-nil :default_shard (intentional single-tenant allowed). [boot guard batch]
| 17 | FIXED | Flush writes x-amz-meta-fathom-md5; download verifies against it regardless of etag form (survives steal-touch multipart rotation).
| 18 | FIXED | check_hrana_exposure! warns in prod when Hrana enabled + auth disabled + wildcard bind. [boot guard batch]
| 19 | FIXED | Dead-incarnation tracking is a MapSet, not a single slot — every proven-dead predecessor stays stealable.
| 20 | PENDING | Eviction probe flips busy bystander to draining |
| 21 | FIXED | flush_then_drop stamps the new etag into the sidecar before drop_local — no spurious FORKED quarantine on a crash after upload. Residue divergence test.
| 22 | FIXED | Usage poll now supervised async_nolink under Fathom.Admin.TaskSupervisor + overlap guard + timeout kill.
| 23 | FIXED | parse_labels total (drops malformed pairs) + parse_line rescue — honors 'never a crash'.
| 24 | FIXED | fmt_rate rounds n*10 then div/rem — fraction carries into whole (9.96→10.0).
